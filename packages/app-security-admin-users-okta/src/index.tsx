import { PluginCollection } from "@webiny/plugins/types";
import { setContext } from "apollo-link-context";
import {ApolloLinkPlugin} from "@webiny/app/plugins/ApolloLinkPlugin";

export default (options: any): PluginCollection => {
    return [

    ];
};
